<%= form_for @check_in, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :descricao, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :descricao, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :detalhes, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :detalhes, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :validade, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.date_select :validade, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  check_ins_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
