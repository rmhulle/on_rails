<%= simple_form_for(@cidade_atuacao) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :cidade %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
